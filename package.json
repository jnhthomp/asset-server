{
  "name": "asset-server",
  "version": "1.0.0",
  "description": "Simple server to hold assets related to my work such as images",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "JTDEV",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.1",
    "express": "^4.18.1"
  }
}
